# To do

- Show reposts in the feed. Fader is meant to be a better Following feed, with everything the people you follow do on X, but `toIncomingTweets` in `src/lib/x/normalize.ts` drops reposts (and its comment says the feed is only about what people write). Keep them, and decide how a repost ranks against its author's own tweets.
